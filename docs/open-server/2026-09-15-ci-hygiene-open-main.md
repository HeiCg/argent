# Ticket: CI hygiene on `open/main` — format, lint, unit tests, knip green; checks fire on our branch

Owner asked (2026-09-15) to clean up the red CI. Facts: nothing on the consolidated base
fails now (bench run 34904658366 green both jobs; iOS device test 34904275293 green); the
red runs are historical shakedowns on feature branches (cited as evidence in tickets,
kept). But the repo hygiene workflows (`unit-tests`, `lint`, `format`, `knip`, `lockfile`,
`repo-hygiene`, `tool-description-quality`, `skill-description-quality`, `docs-build`)
trigger only on `main`, so our code has never been checked by them, and a local check
shows they would be red: prettier lists 156 files, all ours (76 `docs/open-server`, 73
`packages/tool-server`, 4 `.github/bench-ci`, 1 each android-device-server /
ios-device-server / docs); eslint has 3 real errors (unused `eslint-disable no-console`
directives) plus 981 parse errors that are a LOCAL environment artefact (`packages/docs`
tsconfig extends `@docusaurus/tsconfig`, not installed in the root `node_modules`) — CI's
`npm ci` installs it.

## Work
1. `npm run format` (prettier --write) once on the branch — expect ~156 files; commit as
   one mechanical commit "style: prettier over the open-server work"; no content changes.
   Check that `docs/open-server/*.md` tables survive (prettier reflows markdown; if a
   results table breaks, add the file to `.prettierignore` under a comment instead of
   hand-fixing numbers).
2. Fix the 3 eslint "Unused eslint-disable directive" errors; run `npm run lint`
   locally only if `@docusaurus/tsconfig` resolves in the worktree (symlinked root
   `node_modules` — check `ls node_modules/@docusaurus`); otherwise rely on CI.
3. Triggers: add `open/main` next to `main` in `push.branches` and
   `pull_request.branches` of `unit-tests.yml`, `lint.yml`, `format.yml`, `knip.yml`,
   `lockfile.yml`, `repo-hygiene.yml`, `tool-description-quality.yml`,
   `skill-description-quality.yml`, `docs-build.yml` (paths filters unchanged). Bench
   and device-test workflows stay `workflow_dispatch`-only. Push the YAML over SSH.
4. Fix whatever those runs turn red on the branch: likely `knip` (unused exports in the
   new bench/iOS files — remove or mark), `unit-tests` (vitest `--maxWorkers=2` locally
   first on the packages we touched: tool-server, configuration-core, ios-device-server
   host tests; note any pre-existing failure by proving it on `main`), `docs-build`
   (the new `features/open-ios-device-server.mdx` + configuration row must build —
   `npx docusaurus build` runs in CI), `tool-description-quality` (we added/changed tool
   descriptions for iOS routing — the grader must pass), `lockfile` (must match; the
   root lock was regenerated by the planner). Read each failing log fully and fix all
   at once per workflow; budget: the hygiene workflows are cheap, up to 6 pushes.
5. Do NOT delete any GitHub run (the failed ones are evidence cited in tickets; the
   planner already removed the single uncited one).

## Process
Branch `chore/ci-hygiene-open-main` off `open/main` (HEAD ≥ b5be6a9a), worktree
`../argent-fork-wt-hygiene` (never /tmp; root `node_modules` symlinked; no npm install /
gradle / Xcode; vitest `--maxWorkers=2`). Wait for the two bench agents (`wt-ios2`,
`wt-3o`) before touching shared files they own? No — hygiene touches formatting across
the tree; to avoid conflicts, do NOT format files under `packages/ios-sim-input/`,
`bench-ios-*`, `merge-blocks-ios.js`, `scoreboard-ios.js`, `bench-fling*`, and the
3o/iOS-2 docs; those get formatted when their branches merge (say so in the Result).
Polling of hygiene runs: they finish in minutes; one `gh run list -L 10` per 10 min max.
Append `## Result` here: commits, per-workflow outcome on the branch with run ids, what
was fixed, files excluded from formatting and why. Do not fast-forward `open/main`.
